[package]
name = "prime"
version = "0.1.0"
edition = "2024"

[features]
dual_sync = []

[dev-dependencies]
rstest = "0.26.1"

[dependencies]
clap = { version = "4.5.60", features = ["derive"] }
tokio = { version = "1.50.0", features = ["full"] }
